import React from 'react'

type Props = {
    isResources?: boolean
}

export default function Footer({
    isResources = false
}: Props) {
    return (
        <footer>
            
        </footer>
    )
}