'use client'

import React, { isValidElement } from 'react'

type Props = {
    columns: number;
    
}

export default function MegaMenu({ }: Props) {
    return (
        <div>megamenu</div>
    )
}