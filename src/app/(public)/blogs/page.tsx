import React from 'react'

import BG_PATTERN from '@/assets/bg_pattern.svg'
import style from './page.module.scss'

type Props = {}

export default function page({ }: Props) {
    return (
        <div className={`bg relative min-h-screen`}>

        </div>
    )
}