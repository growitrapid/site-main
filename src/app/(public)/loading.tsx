import MainLoader from "@/components/loader";

export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <div className='main-content-loading-234frg'>
        <MainLoader />
    </div>
}