import React from 'react'

type Props = {}

export default function page({ }: Props) {
    return (
        <div className={`relative h-full flex flex-col items-center justify-center`}>
            
            <h1 className={`text-xl font-bold [font-family:var(--font-barlow)]`}>
                Page Not Found
            </h1>

        </div>
    )
}